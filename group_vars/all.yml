---
# Global defaults for VPS bootstrap. Override per-host/per-group as needed.

timezone: UTC

# Bootstrap identity
bootstrap_user: pablo
bootstrap_user_shell: /bin/bash
bootstrap_user_passwordless_sudo: true
bootstrap_user_ssh_public_key: ""
bootstrap_user_ssh_public_key_path: "~/.ssh/hostinger_ed25519.pub"

# SSH hardening policy
ssh_disable_password_auth: true
ssh_permit_root_login: true

# Firewall baseline for web servers
ufw_allowed_tcp_ports:
  - 22
  - 80
  - 443

# Hardening toggles
enable_fail2ban: true
enable_auto_updates: true

# Cross-distro baseline packages
common_packages:
  - curl
  - git
  - neovim
  - htop
  - tmux
  - ripgrep
  - nodejs
  - npm
  - rsync
  - unzip
